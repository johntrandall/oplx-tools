[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "oplx-tools"
version = "0.1.0"
description = "Generate, lint, and parse OmniPlan .oplx files. Companion to the oplx-format spec."
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "oplx-tools contributors" }]
requires-python = ">=3.11"
keywords = ["omniplan", "oplx", "project-management", "gantt", "file-format"]
classifiers = [
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Office/Business :: Scheduling",
  "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
  "PyYAML>=6.0",
  "lxml>=4.9",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov", "ruff", "mypy"]

[project.scripts]
oplx = "oplx.cli:main"

[project.urls]
Spec = "https://github.com/USER/oplx-format"
Source = "https://github.com/USER/oplx-tools"
Issues = "https://github.com/USER/oplx-tools/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/oplx"]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.11"
strict = true
